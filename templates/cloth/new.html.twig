{% extends 'base.html.twig' %}

{% block title %}New Cloth{% endblock %}

{% block body %}
    <div class="container col-md-8">
        <h1>Create new Cloth</h1>

        {{ form_start(form) }}
        {{ form_widget(form) }}
            <button class="btn btn-main mt-2 mb-1">{{ button_label|default('Save') }}</button>
        {{ form_end(form) }}

        <a href="{{ path('app_cloth_index') }}" class="btn btn-main mt-1 mb-5">back to list</a>
    </div>
{% endblock %}
